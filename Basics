#include <bits/stdc++.h>
#include <vector>
using namespace std;
int main() 
{
   vector<int> v;
  for(int i=0;i<5;i++)
  { int x=i;
  cin>>x;
    v.push_back(10*x);
  }
  v.push_back(15);
  v.erase(v.begin());
  cout<<"size "<<v.size()<<endl; 
  cout<<v.front()<<endl;
cout<<"Position"<<v.at(2);
}
