#include<bits/stdc++.h>
#include<algorithm>
#include<vector>
using namespace std;
int main()
{
  vector<int> v;
  int n,i,x;
  cin>>n;
  for(i=0;i<n;i++)
  { 
    x=i;
    cin>>x;
    v.push_back(x);  
  }
  cout<<endl;
  sort(v.begin(),v.end());
  for(i=0;i<n;i++)
  {
    cout<<v[i]<<" ";
  }
}
